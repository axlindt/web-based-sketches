import { defineConfig } from "vite";

export default defineConfig({
  base: "/web-based-sketches/sketch-4-spacefish-disco/",
  build: {
    outDir: "../docs/sketch-4-spacefish-disco",
    emptyOutDir: true,
  },
});